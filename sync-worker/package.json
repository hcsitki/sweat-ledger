{
  "name": "sweat-ledger-sync-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute sweat-ledger-sync --remote --file=schema.sql",
    "db:apply:local": "wrangler d1 execute sweat-ledger-sync --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250516.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.0.0"
  }
}
